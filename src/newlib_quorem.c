/*
 * newlib 1.8.2 libc/stdlib/dtoa.c, vendored verbatim, for quorem
 * (byte-exact). The TU's _dtoa_r is NOT the registered one -- the
 * in-tree compile is 1009 diff words, worse than the libc.c
 * transcription effort -- and stays unregistered here.
 */
#include "dtoa.c"
