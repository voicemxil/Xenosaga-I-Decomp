/*
 * newlib 1.8.2 libc/stdlib/mprec.c, vendored verbatim. All 17
 * functions are byte-exact, _ulp / _d2b / _ratio / _mprec_log10
 * included -- the near-misses that note used to list were libc.c's
 * hand transcriptions of the same functions, not these.  Those have
 * been deleted; this file owns them.
 */
#include "mprec.c"
