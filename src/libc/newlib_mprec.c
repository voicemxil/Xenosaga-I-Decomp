/*
 * newlib 1.8.2 libc/stdlib/mprec.c, vendored verbatim. _pow5mult is
 * byte-exact; _ulp (2d), _d2b (5d), _ratio (9d) and _mprec_log10
 * (10d) are near-misses and stay unregistered.
 */
#include "mprec.c"
