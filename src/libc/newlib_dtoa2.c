/*
 * newlib libc/stdlib/dtoa.c (era rev, vendored verbatim): _dtoa_r --
 * 4,708 bytes, the project's longest-standing WIP -- and quorem, both
 * byte-exact with the full newlib recipe (era headers, -G0, barriers,
 * --expand-sym-loads, gccinc va_arg override). Named dtoa2 because
 * libc.c still carries the old hand-transcription of _dtoa_r's
 * helpers; the source-file field in decompiled.txt picks this object.
 */
#include "dtoa.c"
